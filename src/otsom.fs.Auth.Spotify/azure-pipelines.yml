trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Auth.Spotify/*

pr:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Auth.Spotify/*

extends:
  template: /azure-pipelines.template.yml
  parameters:
    projectName: 'otsom.fs.Auth.Spotify'
