trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Bot/*

pr:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Bot/*

extends:
  template: /azure-pipelines.template.yml
  parameters:
    projectName: 'otsom.fs.Bot'
