trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Resources.Mongo/*

pr:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Resources.Mongo/*

extends:
  template: /azure-pipelines.template.yml
  parameters:
    projectName: 'otsom.fs.Resources.Mongo'
