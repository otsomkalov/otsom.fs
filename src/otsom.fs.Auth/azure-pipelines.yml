trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Auth/*

pr:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Auth/*

extends:
  template: ../../azure-pipelines.template.yml
  parameters:
    projectName: 'otsom.fs.Auth'
