trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Extensions/*

pr:
  branches:
    include:
      - main
  paths:
    include:
      - src/otsom.fs.Extensions/*

extends:
  template: /azure-pipelines.template.yml
  parameters:
    projectName: 'otsom.fs.Extensions'
